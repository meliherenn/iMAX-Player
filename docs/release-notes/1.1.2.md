## iMAX Player 1.1.2

- Connected iMAX QR setup is back on the secure Supabase Edge Function flow.
- Remote setup web form was redeployed to `https://imax-player.netlify.app`.
- Supabase pairing storage now uses short-lived rows behind the Edge Function instead of direct browser table access.
- Turkish public EPG fallback sources were restored for TR channel lists when playlist/Xtream EPG discovery has no usable match.
- EPG fallback and pairing backend were verified with focused automated/synthetic checks.
